import {StyleSheet} from "react-native";

export const chipGroupStyles = StyleSheet.create({
    component: {
        flexDirection: "row",
        justifyContent: "space-between",
        marginTop: 15
    }
})